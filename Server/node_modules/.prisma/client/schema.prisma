generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model movie {
  id       Int     @id @default(autoincrement())
  movie    String
  rating   Decimal
  image    String
  imdb_url String
}
